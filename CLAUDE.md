# คำแนะนำสำหรับ Claude Code

โปรเจกต์นี้ถูกแก้สลับกันระหว่าง **PC (Windows)** และ **MacBook**
ทั้งสองเครื่อง push ได้ ให้ทำตามข้อตกลงเรื่อง git ข้างล่างนี้เสมอ

---

## ⚠️ ข้อตกลงเรื่อง git — ทำเองโดยไม่ต้องรอให้ผู้ใช้สั่ง

**1. ต้น session: ดึงของใหม่ก่อนเสมอ**

```bash
git pull --rebase
```

ทำก่อนแตะไฟล์ใดๆ เพราะอีกเครื่องอาจแก้อะไรไว้

**2. หลังแก้เสร็จแต่ละเรื่อง: commit แล้ว push ทันที**

```bash
git add -A && git commit -m "..." && git push
```

**อย่าปล่อยงานค้างไว้โดยไม่ push** เพราะผู้ใช้จะสลับไปอีกเครื่องเมื่อไหร่ก็ได้
ถ้าค้างไว้ อีกเครื่องจะแก้ทับแล้วชนกัน

**3. ถ้า `git pull --rebase` แล้วชนกัน** — หยุด บอกผู้ใช้ว่าชนตรงไหน อย่าเดา

**4. ก่อนจบการสนทนา** เช็คว่า `git status` สะอาดและไม่มี commit ค้าง

---

## กติกาการแก้โค้ด

**แก้ที่ `src/trip.html` เท่านั้น** — `public/index.html` เป็นไฟล์ที่ถูก generate
ถ้าแก้ตรงนั้นจะโดนทับตอน build

```bash
python3 tools/build.py                        # สร้าง public/ จาก src/
python3 tools/mockserver.py public 8801       # เซิร์ฟเวอร์ทดสอบ → 127.0.0.1:8801
python3 tools/mockserver.py public 8801 down  # จำลองกรณี API ล่ม
```

**ทดสอบทุกครั้งก่อน push** เพราะ push แล้ว Cloudflare deploy ขึ้นเว็บจริงทันที
(อดีตเคยพลาด: ลืมใส่ลูกน้ำคั่นระหว่าง object ใน `DAYS` ทำให้ JS พังทั้งไฟล์และหน้าเว็บว่างเปล่า
เช็คว่าหน้าเรนเดอร์ได้จริงและ console ไม่มี error เสมอ)

### ตัวแปรหลักใน `src/trip.html`

| ตัวแปร | คืออะไร |
|---|---|
| `DAYS` | แผนรายวัน |
| `TRANSPORT` | คู่มือรถไฟฟ้า |
| `PREP` | K-ETA อากาศ เงิน ภาษา |
| `BUDGET` | ตารางงบ |
| `CHECKS` | เช็คลิสต์ |

ใส่ `map:'ชื่อเกาหลี'` ในจุดไหน จุดนั้นได้ปุ่มนำทาง + โชว์คนขับอัตโนมัติ

---

## ข้อมูลค่าใช้จ่ายเป็นของจริง

`https://korea-trip-app.pages.dev/api/state` เป็น **ข้อมูลจริงที่ผู้ใช้บันทึกไว้**
อย่าเขียนทับหรือล้างทิ้งโดยไม่ได้รับอนุญาต — ทดสอบให้ใช้ mock server ในเครื่องแทน

---

## บริบทของทริป

อ่าน **`README.md`** หัวข้อ *"สถานะปัจจุบัน"* — มีงานที่เสร็จแล้ว
เรื่องที่ยังค้าง และราคาโรงแรมที่เช็คไว้

ผู้ใช้สื่อสารเป็นภาษาไทย
